import { ArrowLeft, Heart, Minus, Plus, ShieldCheck, ShoppingBag, Truck } from "lucide-react";
import { useParams } from "react-router";
import { MobileDock } from "../../components/navigation/MobileDock";
import { SiteHeader } from "../../components/navigation/SiteHeader";
import { catalogProducts } from "../../data/catalog";
import "./product.css";
const money=new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0});
export function ProductPage(){const {id}=useParams();const product=catalogProducts.find(item=>item.id===id)??catalogProducts[0];return <div className="product-page"><SiteHeader/><main className="q-container product"><a className="product__back" href="/search"><ArrowLeft size={17}/> Back to collection</a><div className="product__layout"><section className="product__gallery"><img src={product.image} alt={product.name}/></section><section className="product__info"><p className="product__category">{product.category}</p><h1 className="q-display">{product.name}</h1><p className="product__description">{product.description}</p><div className="product__price"><strong>{money.format(product.price)}</strong><s>{money.format(product.mrp)}</s></div><div className="product__purchase"><div className="product__quantity" aria-label="Quantity"><button aria-label="Decrease quantity"><Minus size={16}/></button><span>1</span><button aria-label="Increase quantity"><Plus size={16}/></button></div><button className="product__add"><ShoppingBag size={18}/> Add to bag</button><button className="product__heart" aria-label="Add to wishlist"><Heart size={19}/></button></div><div className="product__assurances"><div><Truck size={20}/><span><strong>Thoughtful delivery</strong>Securely packed for makers.</span></div><div><ShieldCheck size={20}/><span><strong>Quality selected</strong>Materials chosen with care.</span></div></div></section></div></main><MobileDock/></div>}
